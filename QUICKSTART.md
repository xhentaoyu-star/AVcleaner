# AVcleaner v0.8.0 快速开始

AVcleaner 是本机文件名预览和安全改名工具。扫描、规则预览、AI 智能预览都不会修改文件；只有点击“执行选中”并再次确认后，文件才会被改名或隔离。

隔离不是永久删除；需要恢复时，先做回滚预览，再从历史详情里执行回滚。

## 最小流程

1. 启动桌面版，点击文件夹按钮选择文件夹；浏览器模式下可以手动粘贴路径。
2. 选择“规则预览”。如果已经配置 LLM，主界面会额外显示“AI 智能预览”。
3. 点击“分析”。AVcleaner 会一次完成扫描、生成预览、校验并显示表格。
4. 只看表格里的状态、原文件、最终文件名、来源、摘要和操作。
5. 需要完整解释时，点右侧详情图标。
6. 选择安全项，或只勾选自己确认过的项目。
7. 查看执行摘要，再执行选中项。
8. 执行后才会出现执行报告。
9. 需要恢复时，在历史详情里先做回滚预览，再执行回滚。

## 日常模式和排障模式

- 默认是日常模式：界面只显示关键动作和关键判断。
- 高级诊断通过隐藏快捷键打开，会显示 Scan ID、Plan ID、完整 hash、Trace、稳定错误码、能力旗标、运行诊断和脱敏 JSON。
- 快捷键：`Ctrl+Shift+D` 切换高级诊断。
- 这个状态只保存在本机浏览器界面状态里，不进入设置导出，也不会发送给 LLM。

## v0.8.0 工作台

- 默认桌面窗口是 1440 x 900，最小 1280 x 760。
- 顶部命令区只保留路径、文件夹按钮、预览模式、分析、刷新和重新校验。
- 摘要卡默认只保留：总文件、改名、隔离、已选、阻止。
- 警告、需复核、手动修改、关联文件只在有值或筛选相关时出现。
- 表格只放短信息；完整路径、Trace、AI 原因、稳定错误码和 Debug JSON 都在右侧详情或高级诊断里。
- 设置页按 LLM / 规则 / 导入导出 / 诊断 分组，先显示人能看懂的摘要，原始 JSON 默认折叠。
- 执行区不会在执行前显示空的 run_id/status/result 大框。

## AI 智能预览

- 未配置 LLM 时，主流程不显示 AI 模式。
- Strict OpenAI JSON Schema、Prompt JSON compatibility、Claude gateway compatibility 和 Ollama 都只是兼容模式，不会绕过校验。
- AI 建议只写入预览里的最终文件名，不会执行文件。
- AI 输出必须通过结构校验和文件名安全校验。
- 无效 AI 建议会回退到规则结果，并显示稳定错误码。
- 手动编辑优先；AI 不会覆盖手动改过的最终文件名。
- 默认不会把完整本地路径发送给云端 LLM。

## 安全边界

- 扫描和预览不会改名、隔离、删除或移动文件。
- 执行必须使用后端保存的 `plan_id`、匹配的 `plan_hash`、选中的项目 ID 和明确确认。
- 执行不会信任前端传来的本机路径。
- 已存在文件不会被覆盖。
- 回滚不会覆盖目标文件。
- 旧 `POST /api/execute` 保持禁用，错误码是 `legacy_execute_disabled`。
- 通用 `POST /api/llm/suggest` 保持禁用，错误码是 `legacy_llm_suggest_disabled`。

## 便携版

解压 `AVcleaner-v0.8.0-portable-win-x64.zip` 后运行 `AVcleaner.exe`。有 `portable.flag` 时，数据保存在程序旁边的 `data`、`logs`、`quarantine`。没有便携标记时，Windows 默认使用 `%LOCALAPPDATA%\AVcleaner`。

AVcleaner 不做刮削、封面下载、NFO 生成、最终媒体库移动、演员/片商/分类整理，也不接 OpenAver 数据库。
